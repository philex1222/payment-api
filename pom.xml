<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.13</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.example</groupId>
    <artifactId>payment-api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>payment-api</name>
    <description>Payment API project</description>
    <properties>
        <java.version>21</java.version>
        <h2.version>2.4.240</h2.version>
        <rest-assured.version>6.0.0</rest-assured.version>
        <jjwt.version>0.13.0</jjwt.version>
        <springdoc.version>2.8.17</springdoc.version>
        <!--
          Cucumber 7.22.1 is the latest version compatible with JUnit Platform 1.12.x
          (shipped by Spring Boot 3.5.x BOM). Cucumber 7.23+ bumped junit-bom to 5.13+
          (JUnit Platform 1.13), which introduces DiscoveryIssueReporter — a class not
          present in 1.12.x, causing a NoClassDefFoundError at test discovery time.
          Do NOT upgrade beyond 7.22.1 until Spring Boot upgrades JUnit Platform to 1.13+.
        -->
        <cucumber.version>7.22.1</cucumber.version>
        <resilience4j.version>2.4.0</resilience4j.version>
        <!--
          Pin Tomcat ahead of the Spring Boot 3.5.13 BOM (ships 10.1.53).
          10.1.54 fixes all HIGH CVEs found by Trivy as of 2026-04-15:
            CVE-2025-48988 (fixed 10.1.42) — multipart DoS
            CVE-2025-52520 (fixed 10.1.43) — integer overflow multipart
            CVE-2025-53506 (fixed 10.1.43) — HTTP/2 resource exhaustion
            CVE-2025-48989 (fixed 10.1.44) — HTTP/2 MadeYouReset DoS
            CVE-2025-55752 (fixed 10.1.45) — path traversal + RCE via rewrite
            CVE-2026-24734 (fixed 10.1.52) — OCSP revocation bypass
            CVE-2026-34483 (fixed 10.1.54) — JsonAccessLogValve info disclosure
            CVE-2026-34486 (fixed 10.1.54) — EncryptInterceptor bypass
            CVE-2026-34487 (fixed 10.1.54) — sensitive data in log files
          Keeping the explicit property so any future Trivy gate failure makes
          the intended version immediately visible in pom.xml.
        -->
        <tomcat.version>10.1.54</tomcat.version>
        <!--
          Override Flyway within the 11.x line (Spring Boot BOM ships 11.7.2).
          11.8.x and 11.9.x are minor bug-fix releases with no breaking API changes.
          Flyway 12.x is a major version and requires separate assessment before adoption.
        -->
        <flyway.version>11.9.2</flyway.version>
        <!--
          Override httpclient5 ahead of the Spring Boot BOM (ships 5.5.2).
          5.6 is the latest GA release with connection-pool and TLS improvements.
          Same major version — API-compatible.
        -->
        <httpclient5.version>5.6</httpclient5.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Resilience4j BOM — not managed by Spring Boot parent -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bom</artifactId>
                <version>${resilience4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!--
              Pin commons-lang3 ahead of the Spring Boot BOM (ships 3.17.0).
              3.18.0 fixed CVE-2025-48924 (StackOverflowError via ClassUtils.getClass).
              3.20.0 is the latest release — no new CVEs, minor bug-fixes only.
              Transitive via springdoc → swagger-core-jakarta → commons-lang3.
            -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>3.20.0</version>
            </dependency>
            <!--
              Pin Jackson 3.x (tools.jackson) ahead of logstash-logback-encoder:9.0
              which pulls in tools.jackson.core:jackson-databind:3.0.1.
              3.1.1 fixes all HIGH CVEs found by Trivy as of 2026-04-15:
                CVE-2026-29062  (fixed 3.1.0) — DoS via excessive JSON nesting
                GHSA-2m67-wjpj-xhg9 (fixed 3.1.1) — document length constraint bypass
              3.1.2 is a patch release with additional bug fixes; upgrading from 3.1.1.
              jackson-databind and jackson-core must be co-versioned.
            -->
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>3.1.2</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>3.1.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- JWT (split JARs — api at compile, impl+jackson at runtime) -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

        <!-- API Documentation (SpringDoc replaces Springfox — compatible with Spring Boot 3) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <!-- Utilities -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Resilience (version managed by Spring Boot 3 BOM via resilience4j-bom) -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-ratelimiter</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-circuitbreaker</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-retry</artifactId>
        </dependency>

        <!-- Caffeine cache — bounded in-memory cache for rate-limiter buckets -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- Structured JSON logging (Logstash-compatible encoder for ELK / Grafana Loki) -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>9.0</version>
        </dependency>

        <!-- Flyway database migrations (flyway-mysql required for Flyway 10+) -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <!-- Micrometer for metrics (actuator integration) -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Distributed Tracing: Micrometer Tracing bridge to Brave + Zipkin reporter -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-brave</artifactId>
        </dependency>
        <dependency>
            <groupId>io.zipkin.reporter2</groupId>
            <artifactId>zipkin-reporter-brave</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Apache HttpClient 5: SSRF-safe DNS resolver for webhook RestClient + PATCH support -->
        <dependency>
            <groupId>org.apache.httpcomponents.client5</groupId>
            <artifactId>httpclient5</artifactId>
        </dependency>
        <!-- REST Assured — BDD-style HTTP API testing with JSON Schema validation -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-schema-validator</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Cucumber BDD acceptance tests (JUnit 5 integration) -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-spring</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- JUnit Platform Suite — @Suite runner for Cucumber under JUnit 5 -->
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-suite</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                    <!-- Activate 'local' profile by default for mvn spring-boot:run.
                         Override with: mvn spring-boot:run -Dspring-boot.run.profiles=docker -->
                    <profiles>
                        <profile>local</profile>
                    </profiles>
                </configuration>
            </plugin>

            <!-- JaCoCo for test coverage enforcement -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.75</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <excludes>
                        <!-- Exclude generated/boilerplate classes from coverage -->
                        <exclude>com/example/paymentapi/PaymentApplication.class</exclude>
                        <exclude>com/example/paymentapi/DataInitializer.class</exclude>
                        <exclude>com/example/paymentapi/dto/**</exclude>
                        <exclude>com/example/paymentapi/model/PaymentStatus.class</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
